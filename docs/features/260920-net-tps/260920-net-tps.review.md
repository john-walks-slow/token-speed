# 检视报告

## 概要

检视范围：net_tps 指标端到端实现（backend 测速/落库/模型、frontend 类型/统计/行展示、测试）。实现口径正确、链路完整、测试覆盖了纯函数与真实代码路径，整体质量良好；发现 1 处测试类结构被破坏的问题及若干一致性小问题。

## 需求对齐

满足需求。net_tps = content_tokens / (total_latency_ms − content_ttft_ms)，仅流式可测，与需求口径一致：

- 后端成功/失败两路径均携带 net_tps（`speed_test.py:352,380,408`），批量失败兜底路径除外（见建议修改）。
- 落库迁移遵循项目"列追加式"惯例（`database.py:99`），`insert_speed_test` 正确落库。
- 前端统计卡、指标选择器、行展示、类型均已覆盖；失败样本计 0 的口径与 tps 对齐（`StatsPanel.tsx` `metricValue`），非流式样本自动跳过并在注释中说明。
- 实测验证（tps 18.54 / net_tps 52.47）符合预期量级。
- 口径上有一个隐含的正确性确认：content_ttft 取的是首个正文 token 时刻，即使网关隐藏 reasoning（思考时间混入 content_ttft），净 TPS 依然正确扣除思考等待，无口径漏洞。

## 阻塞问题

无。

## 建议修改

| ID | 位置 | 问题 | 建议 |
| --- | ---- | ---- | ---- |
| S1 | /root/projects/token-speed/backend/test_speed_test.py:228-240 | `TestNetTps` 类体直接接在原 `TestMedian:` 的位置，diff 删掉了 `class TestMedian:` 声明行但未在新类后补回，导致 `test_odd` / `test_even` / `test_empty` 三个中位数测试现在挂在 `TestNetTps`（docstring 为"净 TPS 口径"）名下，且方法间无空行，违背 PEP8 与测试分组语义。测试能跑但结构错误。 | 在 `test_non_stream_net_tps_is_none` 之后补回 `class TestMedian:` 声明（前置一个空行），让三个 median 测试归位。 |
| S2 | /root/projects/token-speed/backend/speed_test.py:492-517 | `_batch_error_result` 兜底 dict 缺 `net_tps` 键（`run_speed_test` 的失败路径有）。当前因 `insert_speed_test` 用 `.get()`、`SpeedTestResult` 字段 Optional 而不报错，但同函数内其他键均为显式全量构造，漏键会累积为口径不一致的隐患。 | 在 `_batch_error_result` 中补 `"net_tps": None`，与其他字段保持全量对齐。 |

## 非阻塞问题

| ID | 位置 | 问题 | 建议 |
| --- | ---- | ---- | ---- |
| N1 | /root/projects/token-speed/frontend/src/components/HistoryList.tsx:135、ResultRow.tsx:38 | 行内"净 X"无单位，相邻 tps 显示为 "X tok/s"，读数单位不直观，需依赖 tooltip 理解。 | 若空间允许改为 "净 X tok/s"；空间紧张可保持现状（tooltip 已说明）。 |
| N2 | /root/projects/token-speed/website/patrol/index.html | 巡检看板未展示 net_tps。JSONL 数据（`patrol_runner.py` json_sink 全量 dump）已携带该字段，后续可低成本加入指标选择器。 | 记为后续迭代项，本次不要求。 |
| N3 | /root/projects/token-speed/backend/test_speed_test.py:173-183 | 两个流式/非流式用例通过手动赋值 `st.httpx.AsyncClient` + try/finally 恢复，而非 pytest `monkeypatch` 或 `unittest.mock.patch`；若用例中途断言失败前的异常路径处理不当可能泄漏全局状态（当前 try/finally 已兜住，仅风格问题）。 | 后续可改用 `monkeypatch.setattr(st.httpx, "AsyncClient", FakeClient)` 简化。 |
| N4 | /root/projects/token-speed/backend/database.py | 历史 DB 中已有 content_ttft_ms、content_tokens 列的成功流式样本理论上可回填 net_tps（类似 `_backfill_tps` 的一次性回填）。历史数据量小、且旧口径 content_tokens 可能不准，不回填也合理。 | 备忘：如希望历史看板数据完整，可仿照 `_backfill_tps` 增加一次性回填。 |

## 准入结论

**结论**：条件准入

**说明**：核心口径与链路正确，测试有效；S1（TestMedian 类被 TestNetTps 吞并）应修复后再提交——它改动的是本次 diff 自身，成本极低。S2 及非阻塞项可随本次或后续迭代处理。
