# 网络设置 用户验证

## 验证说明

- 验证对象：代理模式（系统/自定义/关闭）、忽略 TLS 错误开关；深浅色主题首屏与原生控件配色修复
- 环境/前置条件：开发模式（`start.bat`）或桌面 exe 均可；本地代理可选（如 127.0.0.1:7890）

## 验证项

| 验证步骤 | 预期结果 | 实际结果 | 状态 | 备注/证据 |
| -------- | -------- | -------- | ---- | --------- |
| 设置 Tab → 网络：代理模式默认"系统代理"，忽略 TLS 未勾选，保存按钮置灰 | 初始态与默认一致 | | 待验证 | |
| 切换为"自定义代理"→ 显示代理地址输入框 → 填 `http://127.0.0.1:7890` → 勾选忽略 TLS → 保存 | 提示"已保存"；刷新/重启后设置保留 | | 待验证 | |
| 切到测速/服务商检测，执行一次 connect 或测速 | 请求走自定义代理；TLS 错误被忽略（若目标证书异常时不报 SSL 错误） | | 待验证 | |
| 代理模式切"关闭（直连）"→ 保存 → 再测一次 | 请求直连（不经系统代理） | | 待验证 | |
| 切"系统代理"→ 保存 → 再测一次 | 请求跟随系统代理设置 | | 待验证 | |
| 外观：强制浅色 → 刷新/重启 | 首帧即为浅色，无深色闪烁；checkbox/select 控件为浅色外观 | | 待验证 | |
| 外观：强制深色 → 刷新/重启 | 首帧即为深色，无浅色闪烁；控件为深色外观 | | 待验证 | |

## 验证结论

待验证。

## 待跟进

无。
